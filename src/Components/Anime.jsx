
import PropTypes from 'prop-types';
const Anime = ({anime}) => {
    const {id}=anime;
    return (
        <div>
            
        </div>
    );
};

Anime.propTypes = {
    anime : PropTypes.object.isRequired
}

export default Anime;